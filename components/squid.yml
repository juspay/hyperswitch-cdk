name: InstallSquid
description: this document installs node on ubuntu
schemaVersion: 1.0

phases:
    - name: build
      steps:
        - name: InstallSquid
          action: ExecuteBash
          inputs:
            commands:
                - sudo apt-get update -y
                - sudo apt install squid

